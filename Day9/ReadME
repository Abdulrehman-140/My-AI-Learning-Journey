Today I understood underfitting and overfitting. I tried DecisionTreeClassifier() model with max_depth determining the number of questions it can ask. Before adding noise it was simple, If x>0 then y=1 else 0
